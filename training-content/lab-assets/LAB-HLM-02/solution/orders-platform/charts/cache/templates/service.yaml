apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-cache
spec:
  selector: {app: cache, release: {{ .Release.Name }}}
  ports: [{name: redis, port: {{ .Values.service.port }}, targetPort: redis}]
