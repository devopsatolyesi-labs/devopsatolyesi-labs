apiVersion: v1
kind: Pod
metadata:
  name: rejected
  namespace: governed
spec:
  containers:
    - name: memory-heavy
      image: nginx:1.27-alpine
      resources:
        requests: {cpu: 100m, memory: 64Mi}
        limits: {cpu: 250m, memory: 1Gi}
